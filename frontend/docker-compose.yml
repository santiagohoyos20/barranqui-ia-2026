services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        # URL accesible desde el navegador del usuario (no desde el contenedor)
        VITE_API_URL: ${VITE_API_URL:-http://localhost:3000/api/chat}
    ports:
      - "${FRONTEND_PORT:-8080}:80"
    restart: unless-stopped
